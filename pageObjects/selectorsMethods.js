const {By, until} = require("selenium-webdriver");

module.exports = class testPage {
    driver;
    url;
    constructor(myDriver, myUrl){
        this.driver = myDriver
        this.url = myUrl
    }

/* SELECTORS */






/* METHODS */
}